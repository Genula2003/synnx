{
  "name": "synora-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Synora next-generation communication operating system foundation",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "dev": "npm run dev --workspaces --if-present"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "@prisma/client": "^5.22.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "electron": "^29.4.6",
    "framer-motion": "^11.18.2",
    "lucide-react": "^0.469.0",
    "playwright": "^1.62.0",
    "prisma": "^5.22.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "typescript": "^5.3.3"
  }
}
